version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # 这些规则会要求重命名或拆分冻结的旧 wire 类型；兼容边界不得改写它们。
    - IMPORT_USED
    - RPC_REQUEST_RESPONSE_UNIQUE
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
