`sqlcgen/` 目录下的 Go 文件由 sqlc 根据 `queries/persistence.sql`、`session_query.sql`、`workload_bootstrap.sql` 自动生成，修改 SQL 后需重新运行 sqlc 生成；`cmd/genoperationregistry/main.go` 是独立的可执行程序，用于生成 `generated_operation_policies.go`。