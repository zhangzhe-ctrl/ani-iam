- 每层以独立 Go package 隔离职责，依赖方向严格自上而下（server→service→biz←data）。
- biz 层仅声明领域用例与接口，不包含认证规则与存储细节。
- 数据访问通过 Data 结构体持有共享基础设施客户端，并以 WithLifecycleObservation 等方式派生配置副本而非修改原实例。
- 配置集中定义于 conf 包的 protobuf 结构，并在启动前通过 validate.go 进行强校验。