迁移以 `YYYYMMDDNNNN_<主题>.sql` 命名并按时间排序，由 `ani_iam_migrator` 在独立的 bootstrap superuser 会话中依次执行，运行时实例不直接运行迁移。首份 `202609040001_persistence_foundation.sql` 撤销公共权限并创建 principals/tenant_access/tenant_memberships/tenant_roles/tenant_role_bindings/iam_audit_events 等核心表，随后每个迁移增量扩展 schema：认证（password、OIDC）、授权（permission_catalog、platform roles）、工作负载（workload_principals/workload_grants/bootstrap_receipts/outbox）、通知与加密 outbox 等。权限通过显式 `GRANT ... TO ani_iam_runtime` / `ani_iam_provisioner` 控制，provisioner 角色受触发器 `guard_workload_provisioner()` 约束只能创建 workload principal 且仅能写入特定审计事件。`atlas.sum` 记录所有迁移文件的哈希摘要，用于完整性校验；部分迁移（如 permission_catalog）由内部代码生成器产出并附带 SHA-256 来源标记。