每个 WR 目录需从对应 worktree 运行：`python3 tools/wr{17-18,19,20}/remote-run.py --command-file <reviewed-file>`；`wr19/generate.sh` 需在 `$WR19_RUN_DIR/source` 且 Go 1.26.7 环境下执行；所有 heavy 命令必须在远端 `ubuntu` 用户下运行，结果通过同 run 目录下的 `command.pid`/`command.log`/`command.exit` 检查，exit 73/75/76 分别表示锁占用/内存不足/其他构建进行中。