- ADR 使用 `NNNN-短标题.md` 命名并以 YAML front matter 声明 `status`，正文通过具体条款号引用 `decisions.md` 中的已接受或待决项。
- 设计计划顶部以注释块声明自身角色（accepted base design / 历史阶段索引 / 非执行 frontier），并与 spec 和 ticket graph 建立唯一执行入口关系。
- 所有文档遵循权威优先级：用户当前明确决定 > accepted ADR > accepted base decisions，冲突必须显式记录条款与理由，不得静默覆盖。
- 文档区分“已接受”“候选”“历史证据”，未接受的机制不能因出现在文档中即成为目标，必须由被 claim 的事项承载实现。
- 术语统一来自 `CONTEXT.md`，候选词汇在实施前仍须满足 spec、事项与决定登记中的前提，避免同义词漂移。