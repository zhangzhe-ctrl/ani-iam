需通过受控的外部 `go.work` 将 `ani-iam/sdk`、`api`、`workloadregistry` 映射到仓库内源码快照后再构建；运行需两个进程：`./workload-grpc -mode receiver -config receiver.json` 与 `./workload-grpc -mode caller -config caller.json`，且必须存在真实 IAM 与正式的工作负载引导及 Human/API Key 凭证，无离线或 mock 模式。