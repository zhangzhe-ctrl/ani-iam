# 06: 交付目标 Password 身份认证

**What to build:** 为已验证 Human Principal 交付 Password 设置、Argon2id 登录、锁定、重置和明确的 Session 撤销边界。

**Blocked by:** 05 / 证明目标最小纵向链路（Go/No-Go A 已人工接受）

**Status:** resolved

**Type:** enhancement

**Plan mapping:** DP2-2 / password

**Baseline:** 05 接受的目标链路、固定 Authentication 契约和 Password/Session 安全决定。

**Scope:** 一次性 Password Action、Argon2id PHC、登录限流/锁定、账号不枚举、Password Reset、全 Session 撤销接口、Audit 和错误映射。

**Out of scope:** OIDC、完整 Refresh/browser、开放注册、旧 bcrypt 兼容 runtime、生产 Argon2 性能门禁。

**Allowed paths:** `internal/biz/**`、`internal/data/**`、`internal/service/**`、`migrations/**`、`configs/**`、`tests/**`、`go.mod`、`go.sum`，以及本事项和证据目录。

**Forbidden paths:** `api/**`、`deploy/**`、`../ANI/repo/**`；不得自写密码学原语、开放注册、保留明文密码或把未完成的生产 benchmark 标为通过。

**Evidence path:** `.scratch/ani-iam-p2-direct/evidence/06-deliver-password-authentication/`

- [x] Argon2id 参数/PHC/测试向量固定，Secret 不进入日志和证据。
- [x] 成功、无效、锁定、重置、过期/消费 Password Action 行为稳定且不枚举账号。
- [x] Password Reset 撤销该 Human 的全部 Session/Family，普通登录不扩大撤销范围。
- [x] Password/Credential/Session/Grant/Refresh 状态与 Security Audit 在同一 PostgreSQL 事务；Redis 预检失败时不产生登录状态。经 Owner 明确接受，Audit 后 Redis 计数失败可保留保守的失败/Audit 并返回 `503`，登录提交后 Redis reset 失败可保留有界旧桶而不把已提交登录改报失败；不得产生未授权 Session 或凭据成功，且不得宣称 PostgreSQL/Redis 严格原子。

**Verification:** 密码向量、unit、真实数据库/Redis integration、并发锁定、事务回滚、错误映射和敏感信息扫描通过。

**Stop conditions:** 需要自行实现密码学、改变冻结契约、保存明文 Secret 或 Audit 失败仍允许状态提交。

**Recovery:** 清理隔离 Password Credential/Action 和测试 Session；不影响其他 Principal。

## Claim record

- Claimed at: `2026-09-06T15:02:22+08:00`.
- Dependency status: DP2-05 is `resolved` after human acceptance of Go/No-Go A on `2026-09-06`; DP2-01 through DP2-05 are `resolved`, DP2-07 through DP2-14 remain `ready-for-agent`, and no other Direct P2 ticket is `claimed`.
- IAM start: branch `codex/direct-p2-06-14`, starting commit and current HEAD `0f9cb1c73bef12ee7183016ae012c1a52b008de0`, created from clean `main` at that exact commit. The worktree and index were clean before the claim; the only claim-time diff is this ticket record.
- DP2-05 artifacts: IAM implementation `a7620cef9c4374e665ce2aae2015af2e514fbd28`; ANI Gateway implementation `f09a436c6edbd752271d1e4502bbdfd1f1b9e690`; IAM descriptor SHA-256 `df863beb3b095d1f01350c5334d80daf10cdf48083ce0e5663781171aa99a001`; Core descriptor SHA-256 `7dd40f9053b7c1c0c8905decab0f81b07173d0b25651113147bde9a5370d352a`; contract-pins SHA-256 `33376182b2bcd2f0dd7c84bdf9790d492b6a643560a169e80c0fe63e9113c3b9`; policy revision `sha256:f222e2c6d3cd6442449cd722389d3d4fbfcdc7a0fee950c9d28385d3c264affa`.
- Toolchain and pinned dependencies: Go `go1.26.7-X:nodwarf5 linux/amd64`; Docker client `29.7.2`; `github.com/go-kratos/kratos/v3 v3.0.0`; `github.com/alexedwards/argon2id v1.0.0`; `github.com/jackc/pgx/v5 v5.9.2`; `github.com/redis/go-redis/v9 v9.22.0`; `github.com/testcontainers/testcontainers-go v0.43.0`; `github.com/google/uuid v1.6.0`.
- Real dependency artifacts: task-owned PostgreSQL `postgres:16.4-alpine@sha256:5660c2cbfea50c7a9127d17dc4e48543eedd3d7a41a595a2dfa572471e37e64c` with separate migration/runtime roles, and task-owned Redis `redis:7.4-alpine@sha256:ff02b58f971e7d7d156a1267e283fcbbeee91773b6aa36c49dac28ecfe28eadf` with a DP2-06-only namespace. No ANI, Dex, NATS, shared database, deployment, or traffic dependency is authorized for this ticket.
- ANI read-only identity check: existing checkout `/home/chabking/workspace/ANI/repo` is `codex/obs-runtime-p0@5cd7ddedae340ff9eb528210ba1a0d72ee485e93` with unrelated changes; the completed DP2 worktree is `codex/direct-p2-01-05@f09a436c6edbd752271d1e4502bbdfd1f1b9e690` and clean; the safe-merge worktree is `codex/direct-p2-safe-merge@bde4ea72b5a91cd43cc271dd44c09ff262c637e5` and still has unresolved merge conflicts. None is modified or treated as the accepted DP2-08 baseline.
- Allowed paths remain exactly those declared above: `internal/biz/**`, `internal/data/**`, `internal/service/**`, `migrations/**`, `configs/**`, `tests/**`, `go.mod`, `go.sum`, this ticket, and `.scratch/ani-iam-p2-direct/evidence/06-deliver-password-authentication/**`. There is no union with a prior or later ticket. Frozen `api/**`, `deploy/**`, and every ANI worktree remain forbidden.
- Human-approved path extension (`2026-09-06`): after the blocker was presented and narrowed, the user said `继续 DP2-06，恢复 Goal`; this additionally allows only `internal/server/workload_identity.go`, solely to authorize the frozen `RequestPasswordAction` and `CompletePasswordAction` RPCs for the already authenticated Gateway workload. Tests remain under the existing `tests/**` allowance. No other `internal/server/**`, API, config, deploy, ANI, business-logic, or external-state scope is added.

## Confirmed TDD seams

The accepted ticket and frozen contract pre-agree these public seams: `AuthenticationService.PasswordLogin`, `AuthenticationService.RequestPasswordAction`, and `AuthenticationService.CompletePasswordAction`; their framework-independent biz commands/results and consumed ports; the Argon2id adapter; PostgreSQL transaction boundary; Redis account/IP throttle boundary; and stable gRPC status/ErrorInfo mapping. Tests observe behavior at those seams, use known literals/vectors, and do not assert private calls. Unit/fake tests cannot replace the required real PostgreSQL/Redis gates.

## Execution plan

1. Preserve the frozen Proto and extend the existing authentication vertical slice without changing the constructor wiring: existing password, reader, throttle, unit-of-work, token, secret, ID, and clock adapters gain only the behavior required by DP2-06.
2. Add one RED/GREEN slice at a time for thirty-minute single-use setup/reset actions, replacement-token invalidation, non-enumerating request responses, fixed Argon2id PHC creation, expiry, consumption, and replay rejection.
3. Add RED/GREEN slices for normalized-account and source-IP throttling, five-failure/fifteen-minute durable lockout, successful-login reset, uniform invalid-credential responses, and fail-closed Redis/PostgreSQL behavior.
4. Add RED/GREEN slices for atomic Password completion: update/create the credential, consume the action, revoke every Session/Grant/Refresh Family/Refresh Token for only that Human, and append allowlisted Audit in the same PostgreSQL transaction. Cover rollback and concurrent completion.
5. Generate sqlc output from explicit SQL and apply an Atlas checksum-verified migration; do not hand-edit generated output. Run empty-database replay, restricted-role, two-Principal isolation, transaction-failure, Redis-failure, concurrency, and sensitive-value scans against pinned task-owned dependencies.
6. Run targeted and full relevant regression, reproducible generation/diff gates, static/supply-chain checks, and independent Standards and ticket Spec reviews. Fix every blocker, stage only this ticket's paths, commit `feat(dp2-06): deliver password authentication`, record the full SHA, mark the ticket `resolved`, and require a clean worktree before DP2-07.

## Test plan

- Argon2id adapter: fixed version/`m=65536,t=3,p=4`/16-byte salt/32-byte tag, known independent vector/PHC behavior, malformed or non-target PHC rejection, and no plaintext/Secret output.
- Biz/service: known/unknown account indistinguishability, action setup/reset/replacement/expiry/single consumption, account+IP limit metadata, five-failure lockout and success reset, stable gRPC/ErrorInfo, and no business rule in DTO mapping.
- PostgreSQL: empty migration replay with migrator/runtime separation; restricted runtime grants; action/credential/audit atomicity; all-Human Session/Grant/Family/Token revocation; another Principal untouched; forced Audit/database rollback; concurrent token completion has one winner.
- Redis: account and source-IP keys are hashed and isolated, TTL/limit/lock behavior is deterministic, and there is no local fallback. Pre-check outage returns dependency `503` before a login mutation; post-Audit counter-write failure may retain the durable failed-attempt/Audit and return `503`; post-login account-reset failure may retain only the bounded old account bucket while returning the already committed login. Source-IP state is never cleared by account success. Strict durable cross-store coordination/replay remains deferred and `not_verified`.
- Regression/evidence: targeted unit/integration, full Go tests and vet, sqlc/Atlas reproducibility, `git diff --check`, staged path audit, generated/build/temp file audit, and credential/secret scan. Deferred production Argon performance remains `not_verified`.

## Recovery plan

Before commit, retain only Allowed-path diffs and terminate task-owned PostgreSQL/Redis containers; recovery removes only DP2-06 fixtures, database/role, and Redis namespace, then uses an explicit reviewed reverse patch for this branch if needed. After commit, recovery uses a new revert commit against the exact DP2-06 SHA. Never reset, stash, amend, rebase, force, push, touch ANI worktrees, or delete shared/legacy assets.

## Blocker record

- `2026-09-06`: source inventory found that `internal/server/workload_identity.go` authorizes only `PasswordLogin` and `CheckPermission`. The two frozen DP2-06 RPCs would be rejected before their handlers, so service/unit evidence alone cannot establish a delivered runtime. Proceeding requires an exact Allowed-path extension for only `internal/server/workload_identity.go`, solely to register `RequestPasswordAction` and `CompletePasswordAction` in the existing Gateway workload allowlist. Black-box allow/deny coverage will live under the already allowed `tests/**`; no `internal/server/*_test.go`, business logic, API, config, deployment, ANI, or other `internal/server/**` path is requested. DP2-06 remains `claimed`; implementation and RED tests have not started.
- `2026-09-06`: the user resumed the Goal and DP2-06 after the exact one-file request above; the extension is accepted with the same narrow purpose. This clears the blocker and does not authorize any other path or action.
- `2026-09-06`: after the approved workload allowlist slice reached GREEN, source inspection found two further frozen-input gaps. `PasswordLoginRequest` has no end-client source-IP input and the only peer address available to IAM is the immediate mTLS Gateway peer; no trusted Gateway metadata contract exists. Separately, `RequestPasswordAction` returns only an operation ID while completion requires an action token, and current wiring has neither an encrypted notification outbox/key boundary nor a notifier; persisting a raw token would trigger the ticket's plaintext-Secret stop condition. Exact evidence is in `evidence/06-deliver-password-authentication/design-blocker.md`. DP2-06 remains `claimed`; its allowlist slice is `pass`, while source-IP limiting, usable action-token delivery, domain/persistence work, and full gates remain `not_verified`. No API, ANI, composition-root, config-Proto, notification, deployment, or external state was changed.
- `2026-09-06`: blocker audit 2 revalidated the IAM state and found that the parallel ANI task has now received explicit human Merge-Ready acceptance and produced clean integration commit `745ab93817a402b6ebc05de8f79176162c730704`. Read-only inspection of that fixed commit confirmed the Gateway target port/adapter/router still supplies no client address and the public password-action request returns only uniform HTTP `202`. This removes uncertainty about the ANI source but does not expand DP2-06. `evidence/06-deliver-password-authentication/decision-request.md` now gives the recommended additive Proto source-IP contract, exact minimum IAM/ANI paths, and a token-at-dispatch notification outbox that never persists the raw action token, together with the immutable notification inputs and recovery plan still requiring human acceptance.
- `2026-09-06`: blocker audit 3 revalidated the IAM branch/HEAD, expected-only worktree diff, unique claimed ticket, and clean diff check. The concrete notification-service evaluation task `01a074fd-4322-7822-bbda-6a62383f2120` was polled by its live task handle and remains `active/inProgress`, with no completed message, immutable commit, descriptor, or dependency identity available. No user acceptance of Decisions A/B or their path/contract expansions has arrived. Because the same two security inputs have now blocked three consecutive Goal turns and no further aligned implementation is possible without user authority or an external contract, the Goal is marked `blocked`; DP2-06 remains `claimed`, no commit is created, and the existing RED/GREEN/evidence state is preserved for recovery.
- `2026-09-06`: the user explicitly accepted Decision A (an additive internal `PasswordLoginRequest.source_ip` supplied only by a trusted Gateway resolver) and Decision B (token-at-dispatch with no raw action token persisted or logged), then explicitly said to restore and continue DP2-06. The Goal is active again. Work may proceed through all Notification-independent DP2-06 slices; the real Notification client/adapter and cross-service gate still wait for a clean Notification commit, final Proto/descriptor digest, immutable Go dependency, frozen authorization mapping, and Notification L0-L2 evidence. No uncommitted Notification source is consumed as a contract.
- `2026-09-07`: the accepted Decision A adds only `api/iam/v1/authentication_service.proto`, its official generated Go output, `api/iam/v1/iam_descriptor.pb`, and the two contract fixture/pin files to this ticket's path allowance. Decision B allows the transport-neutral dispatcher, durable destination snapshot, lease/retry/CAS state machine, and their tests under the existing biz/data/migration/test paths. It does not yet authorize `internal/conf/**`, `cmd/server/**`, a Notification module dependency, or use of an uncommitted Notification checkout.
- `2026-09-07`: all Notification-independent Password behaviors and the new outbox state machine have targeted unit and real PostgreSQL/Redis evidence. The parallel Notification task `01a0768a-fe65-7491-9884-f729f2fd0e7c` completed its latest turn with `fail / not_verified`, no commit, no freeze evidence, and a dirty `main@2bdc8dbb68c3d309108b57e263359c269627c0aa`. It stopped on its own Goal's mandatory TDD-order rule and requested explicit acceptance of the disclosed Invitation assertion defect plus Password Action/Worker pass-on-first-run exception. DP2-06 therefore remains `claimed`; real adapter, exact error mapping, composition-root wiring, cross-service L0-L2, final gates, review, staging, and commit remain `not_verified`. The Notification worktree was not consumed as a contract.
- `2026-09-07T04:22:25+08:00`: final blocker audit found the same dependency unchanged for the third consecutive resumed Goal turn. Notification task `01a0768a-fe65-7491-9884-f729f2fd0e7c` is terminal/idle with no later turn, clean commit, frozen descriptor/module identity, or post-freeze L0-L2 evidence. All safe Notification-independent DP2-06 implementation and local gates are exhausted; `git diff --check` passes, the index is empty, and no commit has been created. The Goal is therefore marked `blocked` while this ticket remains `claimed`. Recovery state is preserved exactly; no Notification checkout, ANI worktree, deployment, or external state was modified.
- `2026-09-07`: the user reported the Notification clean commit and thereby resumed the blocked Goal. Read-only verification froze remote commit `0e3f0a2b47fcc1fa96fa926cae2b9ab55bd25d84`, Proto SHA-256 `af226602b76ddd7b0cb312456f1845f67d1968eb0228da64fc1cced13a2671b5`, descriptor SHA-256 `7be0a2fa062229717a311af952fc8b3bb7f58c1ef21cde2de741bcbbb4dfc195`, and Go pseudo-version `v0.0.0-20260907002920-0e3f0a2b47fc`. Notification L0-L2 and the Password Action Scope/Recipient/Destination/Intent plus idempotency/error contract are now usable. The IAM data-layer gRPC adapter reached RED then GREEN on that exact module. The root module raises pgx to `v5.10.0` and the Go directive to `1.26.7`; full PostgreSQL regression is still required. Notification itself still has no TLS or trusted producer resolver and returns `WORKLOAD_IDENTITY_MISSING`, so IAM runtime wiring/L3 and DP2-06 completion remain `not_verified`. Exact evidence is in `notification-contract.md`.
- `2026-09-07`: pgx `v5.10.0` passed the full tagged real PostgreSQL/Redis suite in `128.794s`; unit, data race, vet, module verification, tidy-diff, contract pin, and diff check also pass. The IAM outbound Notification client then reached RED/GREEN and proved a real TLS 1.3 gRPC loopback handshake: server identity is checked as DNS `ani-notification`, the service observed the client certificate DNS SAN `ani-iam`, and a wrong server identity failed closed. This stays within `internal/data/**` and does not authorize or simulate the absent Notification resolver. The remaining path and cross-repository checkpoint is unchanged.
- `2026-09-07`: fixed govulncheck v1.7.0 reports zero affected vulnerabilities after importing the Notification module. The regenerated deterministic SBOM has 95 dependency components and SHA-256 `87814ffbec93ed51083a72264f51ef7a07f8096148868582b502563b3ec54072`; the exact first-party Notification commit has no detectable license file, so its license declaration is `not_verified` rather than silently counted as pass. Choosing or adding a license requires an owner decision and a later immutable Notification commit.
- `2026-09-07T09:11:37+08:00`: resumed-run blocker audit 3 revalidated the live Notification task as terminal/idle and the clean external checkout as `main@0e3f0a2b47fcc1fa96fa926cae2b9ab55bd25d84`; its composition still wires `TrustedProducerIdentityMiddleware(nil)` on a non-TLS gRPC listener. IAM has exhausted all safe existing-path work, including a final normal/race regression proving remote error text cannot expose an action token. The same remaining path-authorization, Notification L3, and license-decision condition has therefore repeated for three resumed turns. The Goal is marked `blocked`; DP2-06 remains `claimed`, runtime/L3/staging/commit are `not_verified`, the index is empty, and the unstaged recovery state is preserved.
- `2026-09-07`: the user resumed the Goal and approved the exact IAM composition extension `internal/conf/conf.proto`, `internal/conf/conf.pb.go`, `internal/conf/validate.go`, `internal/conf/validate_test.go`, `cmd/server/app.go`, and `cmd/server/app_test.go`. The same decision authorizes a separate Notification implementation slice for a TLS 1.3 mTLS server plus a transport-owned resolver that maps only a verified client certificate DNS SAN `ani-iam` to the existing `IamPasswordAction` Submit grant. Its real two-process test may use a task-owned ephemeral CA whose certificates and private keys exist only in the test temporary directory. Cluster certificate issuance, Secret mounting, and rotation remain explicitly `not_verified`; no deployment or cluster action is authorized. The user also accepted a documented first-party proprietary/no-license exception for the pinned Notification dependency; this is an owner risk acceptance, not a claim that the dependency has a license.
- `2026-09-07`: final Standards/Spec review closed the unknown-account Audit, exact-clean Notification runtime pin, Redis increasing-delay, and durable-lock account-enumeration findings, but exposed one remaining semantic conflict. PostgreSQL and Redis have no shared transaction: a later Redis failure can leave a conservative durable failure/Audit after a 503, while a reset failure after a committed login can leave the old bounded Redis bucket. Call reordering cannot provide strict zero-partial-state across both stores, and returning 503 after a committed Session would create a retry/duplicate-Session hazard. Exact facts and the recommended owner wording are in `evidence/06-deliver-password-authentication/cross-store-decision.md`. DP2-06 remains `claimed`; final full gates, staging, commit, and DP2-07 are paused for an explicit owner choice between accepting this fail-closed boundary or widening the ticket to a durable login coordination/idempotency subsystem.
- `2026-09-07`: blocker audit 3 revalidated the exact IAM HEAD `0f9cb1c73bef12ee7183016ae012c1a52b008de0`, expected DP2-06-only unstaged recovery state, empty index, passing `git diff --check`, active Goal, and unchanged owner-decision evidence. No acceptance of the conservative fail-closed boundary or authorization to widen DP2-06 to a durable login coordination/idempotency subsystem arrived across three consecutive Goal turns. No further aligned mutation, final gate, staging, commit, or DP2-07 claim is safe under the current ticket. The Goal is therefore marked `blocked`; DP2-06 remains `claimed`, and no push, deployment, cluster action, reset, stash, amend, or cleanup of the preserved worktree is performed.
- `2026-09-07`: the user explicitly approved the recommended boundary with `批准推荐边界：接受安全保守的有限跨存储状态，并继续完整门禁、审查和 DP2-06 提交。` This supersedes only the ticket's former literal zero-partial-state wording. The accepted boundary is: Redis pre-check failure precedes login mutation; a later Redis counter failure may retain conservative PostgreSQL failed-attempt/Audit state while returning `503`; a Redis reset failure after a committed login may retain the bounded old account bucket but does not turn the committed login into an error; neither path may create unauthorized Session or credential success. Strict PostgreSQL/Redis atomicity, durable coordination, repair observability, and login-result replay remain deferred and `not_verified`. No additional path, subsystem, deployment, push, or cluster authority is added. The Goal is active and DP2-06 may continue through full gates, review, exact staging, and local commit.
- `2026-09-07`: final Spec review found that a durable PostgreSQL lock with empty Redis state did not recreate account/IP cooldown, enabling a known/unknown second-request difference. A unit regression first failed with zero Redis failure calls. The corrected locked path performs dummy Argon and records the same account/IP failure. Standards review then found that this first correction omitted the required authentication-failure Audit. A second unit regression first failed with a nil Audit mutation. The final implementation reuses the existing anonymous principal redacted Audit-only UOW path before Redis; it does not change or extend the durable lock, and Audit failure stops before Redis mutation. The real empty-Redis plus durable-lock test passed in `3.148s` and verifies one redacted Audit, unchanged credential failure/lock/version state, uniform first invalid credential, account cooldown, and shared-IP cooldown.
- `2026-09-07`: independent final Standards review `pass` and independent final ticket Spec review `pass`, each with no actionable finding. Exact-final-tree `go test -tags=integration -race ./... -count=1` passed with `tests/integration` in `110.650s`; full normal tests, vet, module tidy/verify, contracts, formatting, diff check, fixed generation, affected-code vulnerability scan, deterministic SBOM, classified Secret scan, and task-container cleanup also pass. Staged-path/diff audit and the local commit remain pending; the ticket stays `claimed` until those finish.
- `2026-09-07`: exact pre-commit staging contains 64 paths, all inside the ticket's base or explicitly approved extensions. The unstaged diff and non-ignored untracked list are empty; cached `diff --check`, allowlist, generated/build/temp artifact audit, and index-snapshot Secret assessment pass. Gitleaks reports the same eight classified non-Secret integrity/test heuristics and the only staged artifact candidate is the intentional deterministic evidence `bom.cdx.json`. The local feature commit remains the only pending closure action.

## Resolution record

- Resolved at: `2026-09-07T13:15:06+08:00`.
- Start SHA: `0f9cb1c73bef12ee7183016ae012c1a52b008de0`.
- Implementation SHA: `74d7e441435d35dc66a733c8bf4a93129b26de3f` (`feat(dp2-06): deliver password authentication`).
- The exact feature index contained 64 approved paths; its staged diff SHA-256 was `9f3f6990657c6bb870c7ac8e6ba53a1788122305c32e59406a59f12cf8170654`. The unstaged and non-ignored untracked sets were empty.
- Full normal and race/integration tests, focused real PostgreSQL/Redis and two-process mTLS tests, vet, module, contract, formatting, generated-output, migration, vulnerability, SBOM, Secret, artifact, staged-path, and diff gates passed. Independent Standards and Spec reviews both passed with no actionable finding.
- `not_verified` remains explicit for production Argon2 performance; cluster certificate issuance, Secret mounting, rotation, deployment, and live traffic; strict PostgreSQL/Redis atomicity and durable coordination/repair/replay; and configured remote config-plugin transport. These are not represented as DP2-06 passes.
- The branch and HEAD were verified at the implementation SHA with a clean worktree immediately after the feature commit. No push, deployment, cluster action, reset, stash, amend, rebase, or force operation was used.
- Recovery, if required, is a new reviewed revert commit against exact implementation SHA `74d7e441435d35dc66a733c8bf4a93129b26de3f`; the committed history is not rewritten.
