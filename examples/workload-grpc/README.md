# Independently runnable gRPC Workload example

This separate Go module uses the public IAM SDK candidate `v0.1.0-rc.1` and the existing Session API `v0.1.0`. It imports no IAM, ANI or Core internal package. Until publication, a controlled external `go.work` must map the API and SDK candidate versions to the reviewed source snapshots; do not add machine-specific replacements to this module.

Build with `go build .`. Run two processes with `./workload-grpc -mode receiver -config receiver.json` and `./workload-grpc -mode caller -config caller.json`. The receiver uses a distinct Workload identity, exact IAM verification Grant, mTLS listener, and the same SDK. A real IAM with formal Workload bootstrap and an authorized Human/API Key credential is required. There is no mock or offline authorization mode.

The receiver checks an example-owned inventory and returns a harmless acknowledgement. It creates **no terminal Session, Redis entry or resource connection**. Full resource acceptance is performed separately using the formal ANI and Session Gateway binaries. Do not deploy this example in place of Session Gateway.

Both config files contain `iam`, `inventory_file`; caller also supplies `target_address`, `target_server_name`, `credential_file`, `request_file`. Receiver supplies `listen_address` (loopback only) and `server_tls`. `iam` follows the public `grpcworkload.ClientConfig` fields: `Address`, `ServerName`, `Environment`, `TrustDomain`, `PolicyRevision`, `TLS` (`CertificateFile`, `PrivateKeyFile`, `CAFile`), optional `Timeout` in nanoseconds, at most two seconds. `server_tls` has the same three file fields. Each process uses its own certificate and key; private files stay outside source control.

The inventory JSON is an array of `{ "tenant_id": "<uuid>", "instance_id": "<resource>", "workload_name": "<workload>", "ready": true }`. It represents data owned by this example, not an IAM identity assertion. A real service replaces it with its own authoritative resource lookup. The request file uses the public Session protobuf JSON representation and contains the already normalized request/idempotency IDs, principal, target, and exec or console options. The caller credential file must be a regular private file (0600); credentials never appear in arguments or output.

A new owner supplies the exact target registration, the mapping from its normalized DTO to authorization dimensions, real resource checks and business implementation. The SDK handles credential minting, request digest, mTLS peer evidence and online verification. Handler code never parses tokens, builds identity metadata, or queries IAM tables. This first reference target is explicitly registered in IAM; adding unrelated targets requires their owner contract and exact Workload Grant registration.
